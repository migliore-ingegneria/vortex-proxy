[package]
name = "vortex-core"
version.workspace = true
edition.workspace = true
authors.workspace = true
description = "Core domain logic and routing models for Vortex"

[dependencies]
arc-swap = "1.6"
async-trait = "0.1"

[dev-dependencies]
criterion = "0.5"
proptest = "1.4"
tokio = { version = "1.0", features = ["rt", "macros", "time"] }

[[bench]]
name = "ewma_benchmark"
harness = false

[lints]
workspace = true
